#include<iostream>
using namespace std;

	int main(){
		
		int liczby[5];
	cout << " Wprowadz 5 liczb dodatnich i ujemnych: ";
	for (int i = 0; i < 5; i++){
		cin >> liczby[i];
	}
	
	cout << " Liczby ujemne zamienione na 0: ";
	if (liczby[i] < 0){
	if (liczby [i] = 0);
	} 
	for (int i = 0; i < 5; ++i){
		cout<<liczby[i] << " ";
	}
	return 0;
	}